@model IEnumerable<DatingProj.Models.Friend>

@{
    ViewBag.Title = "index";
}

<h2>index</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsConfirmed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsConfirmed)
        </td>

    </tr>
}

</table>
